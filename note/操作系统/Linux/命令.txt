●.查询Linux内核和发行版信息
    uname -(a 所有,s 内核,n 完整主机名,r 内核版本,v 内核发行日期,m 机器硬件类型,p 处理器类型,i 硬件平台类型,o 操作系统)

●.用户
    新建用户组: groupadd 组名
        新建rone用户组: groupadd rone
    新建: useradd [-d home] [-c comment] [-g group] [-r] name
        主要参数
            -c：加上备注文字，备注文字保存在passwd的备注栏中。
            -d：指定用户登入时的主目录，替换系统默认值/home/<用户名>
            -g：指定用户所属的群组。值可以使组名也可以是GID。用户组必须已经存在的，期默认值为100，即users。
            -r：建立系统账号。
        新建admin用户: useradd admin
        新建属于用户组rone的用户rone: useradd rone -g rone
    设置密码: passwd -username
        设置admin密码: passwd admin
    删除: userdel -r [username]
        删除admin用户: userdel admin
    用户列表文件：/etc/passwd
        一行记录对应着一个用户，每行记录又被冒号(:)分隔为7个字段，其格式如下：
            用户名:口令:用户标识号:组标识号:注释性描述(用户名):主目录:登录Shell
    用户组列表文件：/etc/group
        格式：
            组名：密码：GID：该用户组中的用户列表
    查看系统中有哪些用户：cut -d : -f 1 /etc/passwd
    查看可以登录系统的用户：cat /etc/passwd | grep -v /sbin/nologin | cut -d : -f 1
    查看用户操作：w命令(需要root权限)
    查看某一用户：w 用户名
    查看登录用户：who
    查看用户登录历史记录：last

●.文件\文件夹的新建、删除、移动、复制等
    新建文件夹
        mkdir [选项] 文件夹名
        选项：
            -p：递归创建所有层级的文件。
                mkdir -p /home/rone/test/20200529
            -m 目标权限：建立目录时，同时设置目录权限。
                mkdir -m 755 test
    新建文件
        touch 文件名
    删除文件夹
        rmdir 文件夹名(tips：只能删除空的文件夹)
    删除文件
        rm [选项] 文件名
            -d: 删除空白文件夹。
            -f: 强制删除文件或文件夹。
            -i: 删除已有文件或文件夹之前先询问用户。
            -r: 递归处理，将指定文件夹下的所有文件和子目录一并处理。
    复制
        cp [选项] 源文件或目录 目标文件或目录(tips：若想要重命名并复制，第二参数为一个文件 cp rone.txt ./snow.txt)
            -r: 递归处理，用来复制文件夹的。
    重命名、移动
        mv 旧文件路径 新文件路径
    创建软连接
        ln -s 源文件 连接文件

●.文件的权限
    ll(ls -l)命令可查看文件的详细信息
        tip：如果系统提示没有该命令，可改用 ls -l，或者编辑 ~/bashrc，增加 alias ll='ls -l'
		按照条件来展示 ll j* 查看以j开头的文件
        详细信息详解：
            一位字母(文件类型)九位字母(操作权限) 文件数量 拥有者 拥有用户组 文件大小 最近修改时间 文件名
            文件类型：
                ”-”,普通文件.
                ”d”目录,字母”d”,是dirtectory(目录)的缩写.
                “l”符号链接。请注意,一个目录或者说一个文件夹是一个特殊文件,这个特殊文件存放的是其他文件和文件夹的相关信息.
                “b”块设备文件。
                “c”字符设备文件。
            九位操作权限，分为三组，分别为拥有者的权限，拥有用户组，其他用户
                r: 读取权限，无为 -
                w: 写权限，无为 -
                x: 执行权限，无为 -
        例如
            drwxr-xr-x  2 root  root  4096 May 28 14:35 test
            权限 文件个数 拥有者 所属的组 文件大小(单位byte) 最优一次修改时间 文件名
    chmod命令用于改变文件或目录的访问权限
        数字设定法
            语法：chmod [-参数] 三位权限数字   文件
            参数：
                -R 　递归处理，将指定目录下的所有文件及子目录一并处理。
            权限数字：rwx权限编程二进制。1：拥有权限，0：没有权限，然后转换成10进制。
                rwx 111 7
                rw- 110 6
                r-x 101 5
                r-- 100 4
                --- 000 0
            第一位权限数字：文件的拥有者、创建者，第二位权限数字：文件拥有者同组的用户，第三位数字：其他用户。
            eg.
                chmod 777 test
        文字设定法
            语法：chmod [-参数] ［who］ ［+ | – | =］ ［mode］ 文件名
            参数：
                -R 　递归处理，将指定目录下的所有文件及子目录一并处理。
            who:
                u 表示“用户（user）”，即文件或目录的所有者。
                g 表示“同组（group）用户”，即与文件属主有相同组ID的所有用户。
                o 表示“其他（others）用户”。
                a 表示“所有（all）用户”。它是系统默认值。
            操作符号:
                + 添加某个权限。
                – 取消某个权限。
                = 赋予给定权限并取消其他所有权限（如果有的话）。
            mode所表示的权限:
                r 可读。
                w 可写。
                x 可执行。
                X 只有目标文件对某些用户是可执行的或该目标文件是目录时才追加x 属性。
                s 在文件执行时把进程的属主或组ID置为该文件的文件属主。方式“u＋s”设置文件的用户ID位，“g＋s”设置组ID位。
                t 保存程序的文本到交换设备上。
                u 与文件属主拥有一样的权限。
                g 与和文件属主同组的用户拥有一样的权限。
                o 与其他用户拥有一样的权限。

    chgrp命令，改变文件或目录所属的组
        语法：chgrp ［选项］ group 文件
        参数：
            -R 　递归处理，将指定目录下的所有文件及子目录一并处理。
        eg.
            chgrp -R rone test

    chown：更改文件属主，也可以同时更改文件属组
        语法：chown ［选项］ 用户|用户:组 文件
        参数：
            -R 　递归处理，将指定目录下的所有文件及子目录一并处理。
        eg.
            chown -R rone test
            chown -R rone:rone test

●.ping [option] ipaddress(IP地址、域名、主机名)
    -c 指定发送的数据包个数
    -f 快速发送。用于网络执行的极限测试
    -s 指定数据包大小。用于测试链路质量和响应速度
    -I 指定发送测试数据包的网络接口
    -R 显示数据包经过的路由过程
    -t 指定数据包的TTL值(TTL用于控制数据包的生命周期)

●.查询已安装的软件包 rpm -q
    rpm -q Name   查找指定的软件包
    rpm -qa       查找所有
    rpm -qi       显示软件包的信息
    rpm -ql       显示软件包的文件列表
    rpm -qs       显示包中文件的状态
    rpm -qd       显示被标注为文档的文件列表

●.查看端口使用情况
    netstat -tunlp|grep 端口号
    根据pid干掉进程：kill -9 进程号

●.历史记录
    history
    history -c #删除，需要在重启后生效
    history -w #删除立即生效
    #设置系统默认的储存数  /etc/profile  HISTSIZE=XX

●.tail命令语法  https://www.cnblogs.com/mfryf/p/3336804.html
    tail [ -f ] [ -c Number | -n Number | -m Number | -b Number | -k Number ] [ File ]
    参数解释：
    -f 该参数用于监视File文件增长。
    -c Number 从 Number 字节位置读取指定文件
    -n Number 从 Number 行位置读取指定文件。
    -m Number 从 Number 多字节字符位置读取指定文件，比方你的文件假设包括中文字，假设指定-c参数，可能导致截断，但使用-m则会避免该问题。
    -b Number 从 Number 表示的512字节块位置读取指定文件。
    -k Number 从 Number 表示的1KB块位置读取指定文件。
    File 指定操作的目标文件名称
    上述命令中，都涉及到number，假设不指定，默认显示10行。Number前面可使用正负号，表示该偏移从顶部还是从尾部開始计算。
    demo:
        tail -f filename        说明：监视filename文件的尾部内容（默认10行，相当于增加参数 -n 10），刷新显示在屏幕上。退出，按下CTRL+C。
        tail -n 20 filename     说明：显示filename最后20行。
        tail -r -n 10 filename  说明：逆序显示filename最后10行。

●.less 查看
    参数：
        -N 显示行号
    进入后：
        回车  向下移动一行
        y     向上移动一行
        空格  向下滚动一屏
        b     向上滚动一屏
        d     向下滚动半屏
        u     向上滚动半屏
        g     第一行
        G     最后一行
        v     打开vi编辑器
        q     退出

●.find 查找文件
    find [OPTIONS] [查找起始路径] [查找条件]
        OPTIONS:-P、-L、-H：控制软连接的对待方式，用的不多。
        查找起始路径:就是个目录路径，相对和绝对都可以。默认当前。
        查找条件:
            根据名称查找:-name/-iname(忽略大小写) 文件名(*:任意内容,?:任意一个字符,[]任意一个中括号内的字符)。
            根据文件从属关系查找:
                -user USERNAME：查找属主指定用户的所有文件；
                -group GRPNAME：查找属组指定组的所有文件；
                -uid UID：查找属主指定的UID的所有文件；
                -gid GID：查找属组指定的GID的所有文件；
                -nouser：查找没有属主的文件；
                -nogroup：查找没有属组的文件；
            根据文件的类型查找:-type：根据不同的文件类型筛选
                f   普通文件
                d   目录文件
                l   符号链接文件
                b   块设备文件
                c   字符设备文件
                p   管道文件
                s   套接字文件
            根据时间戳查找: -[m|a|c][time|min] [-|+|无修饰符]n
                m:文件内容上次修改时间
                a:文件被读取或访问的时间
                c:文件状态变化时间
                time:单位天
                min:单位分钟
                -:距现在 n单位 内修改过的文件
                +:距现在 (n+1)单位 前修改过的文件
                无修饰符:距现在 n单位 前, (n+1)单位 内修改过的文件
                n:整数
            按文件大小搜索:-size [+|-|无修饰符]文件大小（单位为：k,M,G）[-[a|o] -size [+|-|无修饰符]文件大小]
                +:大于
                -:小于
                无修饰符:等于
                [-[a|o] -size [+|-|无修饰符]文件大小]:支持多条件查询
                    a:and
                    o:or
            根据文件的权限来查找:
                find 路径 -perm mode      #实现的是精确匹配
                find 路径 -perm -mode     #完全包含此mode时才可以匹配（转换为权限字符r,w,x后再比较）
                find 路径 -perm /mode     #任何一位匹配即可（即所有者，所属组，其他人权限中的一位）
         查找是忽略没有权限的文件夹，在最后加上 2>/dev/null
            eg. find / -name rone.txt 2>/dev/null

●.ps -ef |grep nginx   查询nginx相关的进程

●.df -hl  查看磁盘剩余空间信息
    Filesystem      Size Used Avail Use% Mounted on
    文件系统          容量  已用  可用  已用%  挂载点　

●.date 查看/设置 系统时间
    date    查看系统时间
    date -s "20210312 17:00:00" 设置系统时间

●.查看端口占用
    lsof -i:端口号
    netstat -tunlp|grep 端口号

●.查看进程号的服务
    ll /proc/进程号/cwd

●.grep用于查找文件里符合条件的字符串
	#查找后缀有“.conf”的文件包含“test”字符串的文件
	grep test *.conf
	
todo:
nohup 命令
tar 命令
su命令
查看host文件配置
    /etc/hosts






